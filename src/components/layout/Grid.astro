---
interface Props {
  minW?: string;
}

const { minW } = Astro.props;
---

<div>
  <slot />
</div>

<style define:vars={{ minW: minW ? minW : "1fr" }}>
  div {
    display: grid;
    gap: 2rem;
    grid-template-columns: repeat(auto-fit, minmax(var(--minW), 1fr));
  }
</style>
